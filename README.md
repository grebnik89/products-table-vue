# Vue 2 Тестовое задание - Менеджер товаров

Одностраничное приложение для управления товарами с добавлением, удалением, перемещением и валидацией с API-запросами.

## 📁 Структура проекта

```text
src/
├── components/          # Vue компоненты
│   ├── ProductForm.vue  # Форма добавления товара
│   ├── ProductTable.vue # Таблица товаров (классы, декораторы)
│   ├── AppSpinner.vue   # Индикатор загрузки
│   └── AppNotification.vue # Уведомления
├── store/               # Vuex хранилище
│   ├── index.ts         # Корневой store
│   └── modules/
│       └── products.ts  # Модуль товаров
├── types/               # TypeScript типы
│   └── product.ts       # Интерфейсы товаров
├── services/            # Сервисы
│   └── api.ts           # Mock API
└── shims-*.d.ts         # Декларации типов
```

## 🚀 Функциональность

- **Таблица товаров** с колонками: ID, Название, Категория, Цена, Количество, Статус, Действия
- **Добавление товаров** через форму с валидацией
- **Удаление товаров** с подтверждением
- **Перемещение товаров** в таблице (кнопки вверх/вниз)
- **Валидация данных** при добавлении товара
- **Сохранение данных** в LocalStorage
- **Mock API** с имитацией задержки и обработкой ошибок
- **Уведомления** об успешных операциях и ошибках
- **Индикация загрузки** при работе с API

## 🛠 Технологический стек

- **Vue 2.6** + TypeScript
- **Vuex 3.6** для управления состоянием
- **Vue CLI 5** для сборки
- **LocalStorage** для persistence
- **Mock API** с setTimeout для имитации бэкенда

## 📋 Требования к окружению

- **Node.js**: 14.x или выше (рекомендуется 16.x)
- **npm**: 6.x или выше (рекомендуется 8.x)

Проверить версии можно командами:
```bash
node --version
npm --version
```

### Установка зависимостей
```bash
npm install
```

### Сборка в режиме разработки
```bash
npm run serve 
# Приложение будет доступно по адресу: http://localhost:8080
```

### Сборка в продакшн
```bash
npm run build
# Собранные файлы будут в папке dist/
```

### Запуск линтера
```bash
npm run lint
```

### Ссылка на документацию по Vue CLI
See [Configuration Reference](https://cli.vuejs.org/config/).
